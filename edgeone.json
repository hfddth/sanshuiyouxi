{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "agents": {
    "framework": "langgraph",
    "dir": "agents",
    "timeout": 900,
    "sandbox": {
      "timeout": 900
    }
  }
}
